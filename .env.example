# Brand Builder Environment Variables
# Copy this file to .env.local and fill in your values

# PostgreSQL Database Configuration (Required)
# Railway provides this automatically when you add a PostgreSQL service
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL=postgresql://postgres:password@localhost:5432/brand_builder

# OpenAI API Configuration
# Get your API key from https://platform.openai.com/api-keys
# Used for brand personality analysis (gpt-5-mini) and data extraction (gpt-4o-mini)
OPENAI_API_KEY=your_openai_api_key_here

# Firecrawl API Configuration (Optional but recommended)
# Get your API key from https://firecrawl.dev/
# Used to capture website screenshots for vision-based brand extraction
# If not set, falls back to CSS parsing
FIRECRAWL_API_KEY=your_firecrawl_api_key_here

